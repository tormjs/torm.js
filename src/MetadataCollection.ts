/**
 * Metadata Collection
 * 
 * @param {any} [typeOrOptions]
 * @param {any} [options]
 * @returns {Function}
 */

export class MetadataCollection<T extends { target?: Function|string }> extends Array<T> {
  // constructor() {

  // }
}